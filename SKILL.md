---
name: translate-explain-pdf
description: >
  Use this skill whenever the user gives a link to a webpage, article, tutorial,
  or documentation page (especially in English) and wants it translated to Arabic,
  explained/simplified point-by-point, and turned into a well-formatted RTL Arabic
  PDF they can study from. Trigger this for phrases like "ترجملي الصفحة دي وحولها
  PDF", "لخصلي واشرحلي الموضوع ده", "عايز أذاكر من اللينك ده بالعربي", "حول المقال
  ده لملف عربي أقدر أذاكر منه", or any request to study/learn from a foreign-language
  webpage or article by converting it into an Arabic learning document. Also use
  this when the user uploads or references a technical/educational English article
  and wants a full translated + explained Arabic version, not just a summary.
  This is NOT for quick one-line translations or for content the user wants to keep
  in English — it is specifically for producing a full study-ready Arabic PDF.
---

# Translate + Explain + PDF (مذاكرة بالعربي)

## الهدف

الشخص بيدي لينك لصفحة (مقال، درس، توثيق تقني...) وعايز ملف PDF عربي، RTL، فيه:
1. **ترجمة كاملة** للمحتوى (مش تلخيص — المفروض نفس التفاصيل).
2. **شرح وتبسيط تحت كل نقطة مباشرة** (مش في قسم منفصل آخر الملف).
3. **المصطلحات التقنية/البرمجية تفضل بالإنجليزي** جوه النص العربي (مش بنترجمها) لأنه هيستخدمها مع فريقه.
4. لو فيه **نقاط عملية/خطوات تنفيذية** → لازم شرح تطبيقي/مثال عملي إضافي تحتها.
5. لو فيه **كود برمجي** → الكود نفسه يفضل زي ما هو بالإنجليزي، وبس التعليقات (comments) جوه الكود تتترجم.
6. الشكل النهائي **منسق بصريًا**: عناوين ملونة، صناديق (boxes) تبرز النقط والتعريفات المهمة، مش نص عادي سادة.

## خطوات التنفيذ (لازم تتبع بالترتيب)

### 1. تجهيز البيئة (مرة واحدة بس لو مش مركبة)

شغّل سكريبت الإعداد الجاهز — بيتأكد من المكتبات والخطوط ويعمل اختبار تصيير سريع، ومتكرر التشغيل بأمان (idempotent):
```bash
bash scripts/setup.sh
```

لو مش شغال (مثلاً مفيش `apt-get` على البيئة)، ثبّت يدوي:
```bash
pip install weasyprint trafilatura --break-system-packages
apt-get install -y fonts-hosny-amiri fonts-sil-scheherazade fonts-kacst
```

### 2. جيب محتوى الصفحة

استخدم `web_fetch` على اللينك اللي الشخص بعته. لو الصفحة طويلة جدًا وحصل truncation، اعمل fetch تاني بأجزاء أو استخدم `text_content_token_limit` أعلى.

**لو web_fetch رجع محتوى مليان navigation/ads/junk**: استخدم trafilatura كـ fallback لاستخراج المحتوى الأساسي فقط:
```python
import trafilatura
downloaded = trafilatura.fetch_url(url)
content = trafilatura.extract(downloaded, include_formatting=True, include_links=False)
```

**مهم بخصوص حقوق النشر**: لو الصفحة عبارة عن مقال محمي بحقوق نشر (مش توثيق تقني رسمي أو محتوى تعليمي مفتوح)، احترم قواعد الاقتباس المعتادة عند أي جزء تقتبسه حرفيًا — بس الهدف هنا هو **إعادة صياغة/ترجمة وشرح بأسلوبك**، مش نسخ حرفي، فده غالبًا مش هيسبب مشكلة. تجنب نسخ نص طويل حرفي زي ما هو حتى لو هتترجمه لاحقًا — افهم المحتوى واكتبه من جديد بالعربي.

### 3. حلل بنية الصفحة

قسّم المحتوى إلى وحدات منطقية (عنوان رئيسي، عناوين فرعية، فقرات، خطوات، أكواد، جداول). حافظ على نفس الترتيب والتسلسل الأصلي — الهدف نسخة عربية موازية للمحتوى الأصلي، مش تلخيص.

### 4. الترجمة + الشرح (الخطوة الأهم)

لكل نقطة/فقرة/خطوة في المحتوى الأصلي:

1. اكتب **الترجمة العربية الكاملة** للنقطة (بأسلوبك، مش نسخ-حرفي من المصدر).
2. **المصطلحات التقنية** (أسماء دوال، مكتبات، أدوات، مفاهيم برمجية زي `API`, `array`, `async`, أسماء لغات/فريمووركس...) — **سيبها بالإنجليزي** جوه الجملة العربية، متترجمهاش لأنه هيستخدمها مع الفريق.
3. **مباشرة تحت كل نقطة** (مش في نهاية الملف)، ضيف صندوق شرح صغير: "💡 يعني إيه؟" أو "🔍 توضيح" يبسط النقطة بأسلوب سهل، خصوصًا لو فيها لبس أو مفهوم مجرد.
4. لو النقطة **خطوة عملية/تطبيقية** (زي "اعمل كذا، ثم كذا")، ضيف مثال تطبيقي أو توضيح لسيناريو استخدام حقيقي في صندوق منفصل.
5. **الأكواد البرمجية**: اعرض الكود زي ما هو بالظبط بالإنجليزي (بدون أي تغيير في الكود نفسه). لو فيه تعليقات (`// comment` أو `# comment`) جوه الكود، ترجم التعليقات بس واسيب باقي الكود زي ما هو. حط الكود جوه `<pre><code>` بمحاذاة LTR حتى لو باقي الصفحة RTL (عشان الكود يتقرأ صح).

**⚠️ قاعدة حرجة لتجنب تكسير النص (bidi bug): ممنوع تترجم كومنت فيه quotes أو رموز أو أسماء تقنية مختلطة مع عربي.**
جرّبنا حلول زي فصل الأسطر أو الـ Unicode isolate characters (`U+2066`/`U+2069`) وطلعت مش موثوقة 100% — لسه بتحصل حالات تقليب في ترتيب الكلمات، خصوصًا لما يبقى فيه quotes ("...") أو رموز زي `*` `-` `/` أو أسماء دوال/متغيرات جنب الكلام العربي في نفس السطر. الحل النهائي الموثوق:

- **لو الكومنت جوه الكود فيه أي حاجة من دي: quotes حوالين قيمة، رمز خاص (`*`, `-`, `/`, `_`...) ملتصق بكلمة، اسم متغير/دالة/كوماند تقني — سيبه بالإنجليزي زي ما هو بالظبط، متترجموش خالص.** الشرح العربي المبسط لمعنى الكومنت ده هيبقى موجود أصلاً في `explain-box` أو `practical-box` تحت الكود مباشرة، فمفيش فقدان للمعنى.
- **ترجم بس الكومنتات اللي عبارة عن جملة عربية سليمة بدون أي توكن LTR مختلط جواها** (زي `# add a new entry` → `# إضافة عنصر جديد`) — دي آمنة لأنها مفيهاش تبديل اتجاه في نص السطر.
- لو مش متأكد إن الكومنت هيترجم بأمان، الافتراضي الآمن هو **سيبه بالإنجليزي**.

**لا تلخص ولا تختصر أي جزء من المحتوى الأصلي.** لو الصفحة طويلة، خد وقتك واشتغل section بعد section بدل ما تحاول تعمل كل حاجة مرة واحدة.

**قاعدة أساسية: ممنوع حذف أو دمج أي تكرار موجود في المصدر، حتى لو شكله "زيادة عن الحاجة".** لو الصفحة فيها نفس الكود بأكتر من لغة برمجة (Python, Node.js, Java, Go...) أو نفس المثال بصيغ متعددة — لازم تحط **كل نسخة كاملة زي ما هي بالظبط** في الـ PDF، بدون اختيار "اللغة الأشهر" أو حذف باقي اللغات لتوفير وقت. الشخص اللي بيدرس محتاج يشوف كل اللي في الصفحة الأصلية، مش نسخة مختصرة منها. الاستثناء الوحيد المسموح: مش لازم تكرر **الشرح العربي/التبسيط** لكل لغة على حدة لو المفهوم نفسه — يكفي شرح واحد للمفهوم، وتحته كل نسخ الكود (بكل اللغات) كل واحدة في `code-block` منفصل ومعنون باسم اللغة.

### 5. ابني ملف الـ HTML

استخدم القالب في `assets/template.html` كنقطة بداية (فيه CSS جاهز لـ RTL، صناديق ملونة للشرح، تنسيق للكود). املأ فيه المحتوى المترجم مع الحفاظ على البنية:

- كل نقطة رئيسية → `<div class="point">` فيها الترجمة، وتحتها مباشرة `<div class="explain-box">` فيها الشرح المبسط.
- النقط العملية → استخدم `<div class="practical-box">` بدل `explain-box`.
- الأكواد → `<pre class="code-block"><code>...</code></pre>`.
- استخدم `<h1>`, `<h2>`, `<h3>` لعناوين الأقسام (فيهم تنسيق ألوان جاهز في الـ CSS).

راجع `assets/template.html` وملف `references/design_notes.md` لتفاصيل الكلاسات المتاحة قبل ما تكتب الـ HTML.

### 6. حوّل الـ HTML لـ PDF

```python
from weasyprint import HTML
HTML(string=html_content).write_pdf("/mnt/user-data/outputs/اسم_الملف.pdf")
```

أو استخدم السكريبت الجاهز:
```bash
python3 scripts/html_to_pdf.py /path/to/file.html "/mnt/user-data/outputs/اسم_الملف.pdf"
```

تأكد إن اسم الملف بيوصف المحتوى (مثلاً `شرح_مقال_React_Hooks.pdf`) — ممكن تستخدم عربي أو إنجليزي في اسم الملف حسب وضوح المحتوى.

### 7. راجع الناتج

افتح الـ PDF بصريًا (`view` على أول صفحة كصورة لو محتاج تتأكد) وشوف:
- الاتجاه RTL صح والنص مش متقطع أو متراكب.
- الخط العربي ظاهر صح (مش مربعات فارغة) — لو فيه مشكلة خط، تأكد إن `fonts-hosny-amiri` متثبتة.
- الكود ظاهر LTR وسط النص العربي بشكل صحيح.
- الصناديق والألوان شكلها كويس ومش طافحة برا الصفحة.

### 8. سلّم الملف

استخدم `present_files` لعرض الـ PDF النهائي للشخص، مع سطر مختصر واحد بس (بدون شرح طويل بعد الرابط).

## ملاحظات مهمة

- **متختصرش المحتوى أبدًا** — الهدف نسخة كاملة مذاكرة، مش ملخص.
- **متترجمش المصطلحات التقنية/البرمجية** — سيبها إنجليزي جوه الجملة العربية.
- **الشرح دايمًا تحت النقطة على طول**، مش في قسم مصطلحات منفصل آخر الملف.
- **الكود يفضل زي ما هو**، الترجمة بس للتعليقات جواه.
- لو الصفحة طويلة جدًا (كتاب كامل أو توثيق ضخم)، اقترح على الشخص نقسمها لأجزاء بدل ما نحاول كل حاجة في رد واحد.
